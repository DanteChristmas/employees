extends layout

block content
  h1= title
  #employees
    if employees
      - each employee in employees
        div.employee
          div.created_at= employee.created__at
          div.title= employee.title
          div.name= employee.name
    a(href="/employee/new")!= "Add New Employee"
